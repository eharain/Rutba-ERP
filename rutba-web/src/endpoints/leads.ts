/**
 * WebLeadsEndpoints
 * Public lead-capture endpoint mapping.
 */
export const WebLeadsEndpoints = {
  /** Create CRM lead from website form. */
  create: () => ({
    path: "crm-leads",
  }),
};
