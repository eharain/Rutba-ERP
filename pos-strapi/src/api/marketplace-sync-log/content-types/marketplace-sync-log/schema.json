{
  "kind": "collectionType",
  "collectionName": "marketplace_sync_logs",
  "info": {
    "singularName": "marketplace-sync-log",
    "pluralName": "marketplace-sync-logs",
    "displayName": "Marketplace Sync Log",
    "description": "Audit trail of marketplace sync runs (order pulls, inventory pushes, token refreshes)"
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "marketplace_account": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::marketplace-account.marketplace-account",
      "inversedBy": "sync_logs"
    },
    "platform": {
      "type": "string"
    },
    "kind": {
      "type": "enumeration",
      "enum": [
        "orders",
        "inventory",
        "catalog",
        "oauth",
        "fulfillment",
        "messages"
      ],
      "required": true
    },
    "status": {
      "type": "enumeration",
      "enum": [
        "running",
        "success",
        "partial",
        "error"
      ],
      "default": "running"
    },
    "fetched": {
      "type": "integer",
      "default": 0
    },
    "pushed": {
      "type": "integer",
      "default": 0,
      "description": "Rows sent TO the marketplace (status updates, conversation messages). Distinct from created/updated, which count what changed on this side."
    },
    "created": {
      "type": "integer",
      "default": 0
    },
    "updated": {
      "type": "integer",
      "default": 0
    },
    "skipped": {
      "type": "integer",
      "default": 0
    },
    "failed": {
      "type": "integer",
      "default": 0
    },
    "attention": {
      "type": "integer",
      "default": 0,
      "description": "Rows that completed but need a human look — currently an order created with line items whose SKU matched no product. Deliberately NOT counted as failed: the order was created and is real, it just landed with dangling lines. A run with attention > 0 and no failures is 'partial', not 'success'."
    },
    "error": {
      "type": "text"
    },
    "detail": {
      "type": "json"
    },
    "started_at": {
      "type": "datetime"
    },
    "finished_at": {
      "type": "datetime"
    }
  }
}
