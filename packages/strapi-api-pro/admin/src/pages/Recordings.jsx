import React from 'react';
import { Box, Typography } from '@strapi/design-system';

const Recordings = () => (
  <Box padding={6}>
    <Typography variant="beta">Recordings</Typography>
    <Typography variant="omega">Start/Stop recording controls and session list will be implemented here.</Typography>
  </Box>
);

export default Recordings;
