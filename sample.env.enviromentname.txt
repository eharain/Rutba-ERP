

# .env.development - Development configuration
#
# Used by scripts/load-env.js (file mode) when .env contains ENVIRONMENT=development.
# Required variables are defined in scripts/env-config.js — the loader validates
# them at startup and will error/warn on missing values.
#
# In Docker/CI/Hostinger (env-var mode), set these as real environment variables
# using the same key names. The loader auto-detects the mode.
#
# PORT RESOLUTION (managed by scripts/load-env.js)
#   1. process.env.PORT (platform/host override — ALWAYS wins)
#   2. Explicit PREFIX__PORT in this file (e.g. AUTH__PORT=4003)
#
#   No PORT is auto-derived from URLs. If PREFIX__PORT is not set and
#   the platform does not provide PORT, the app uses its own default.
#
#   On managed platforms (Hostinger, Railway, Azure App Service) the
#   platform sets PORT automatically. Do NOT set PORT yourself.

# GLOBAL VARIABLES (no prefix - available to all apps)

## These are available to all apps (no prefix) and are injected into the build
#NEXT_BUILD_OUTPUT=
#BUILD_DEST_DIR=./build/
#RUTBA_APP=web

NEXT_PUBLIC_API_URL=http://localhost:4010/api/
NEXT_PUBLIC_IMAGE_URL=http://localhost:4010

NEXT_PUBLIC_STOREFRONT_URL=http://localhost:4000
NEXT_PUBLIC_AUTH_URL=http://localhost:4003
NEXT_PUBLIC_STOCK_URL=http://localhost:4001
NEXT_PUBLIC_POS_URL=http://localhost:4002
NEXT_PUBLIC_PORTAL_URL=http://localhost:4004
NEXT_PUBLIC_ORDERS_URL=http://localhost:4013
NEXT_PUBLIC_RIDER_URL=http://localhost:4012
NEXT_PUBLIC_CRM_URL=http://localhost:4005
NEXT_PUBLIC_HR_URL=http://localhost:4006
NEXT_PUBLIC_ESS_URL=http://localhost:4015
NEXT_PUBLIC_ACCOUNTS_URL=http://localhost:4007
NEXT_PUBLIC_PAYROLL_URL=http://localhost:4008
NEXT_PUBLIC_CMS_URL=http://localhost:4009
NEXT_PUBLIC_SOCIAL_URL=http://localhost:4011
NEXT_PUBLIC_MANUFACTURING_URL=http://localhost:4014
NEXT_PUBLIC_MARKETPLACE_URL=http://localhost:4016
NEXT_PUBLIC_CONTROL_URL=http://localhost:4017
NEXT_PUBLIC_SEED_URL=http://localhost:4018
NEXT_PUBLIC_CAMPAIGNS_URL=http://localhost:4019
NEXT_PUBLIC_MAIL_URL=http://localhost:4021
NEXT_PUBLIC_CONSOLE_URL=http://localhost:4022
NEXT_PUBLIC_HELPDESK_URL=http://localhost:4023

NEXT_PUBLIC_IMAGE_HOST_PROTOCOL=http
NEXT_PUBLIC_IMAGE_HOST_NAME=localhost
NEXT_PUBLIC_IMAGE_HOST_PORT=4010

# APP-SPECIFIC VARIABLES (PREFIX__ stripped at runtime)

# ----- services/strapi -----
# Use POS_STRAPI__ prefix for Strapi-only settings (loader strips prefix for services/strapi)
POS_STRAPI__ADMIN_JWT_SECRET=change_me
POS_STRAPI__API_TOKEN_SALT=change_me
POS_STRAPI__APP_KEYS=change_me_app_key_1,change_me_app_key_2
POS_STRAPI__DATABASE_CLIENT=mysql
POS_STRAPI__DATABASE_HOST=127.0.0.1
POS_STRAPI__DATABASE_NAME=rutba_pos
POS_STRAPI__DATABASE_PASSWORD=change_me_db_password
POS_STRAPI__DATABASE_PORT=3306
POS_STRAPI__DATABASE_SSL=false
POS_STRAPI__DATABASE_USERNAME=root
POS_STRAPI__ENCRYPTION_KEY=change_me
POS_STRAPI__HOST=0.0.0.0
POS_STRAPI__JWT_SECRET=change_me
POS_STRAPI__MYSQL_ROOT_PASSWORD=change_me_root_password
POS_STRAPI__PORT=4010
POS_STRAPI__TRANSFER_TOKEN_SALT=change_me
POS_STRAPI__UP_ACCESS_TOKEN_LIFESPAN=15m
POS_STRAPI__UP_MAX_REFRESH_TOKEN_LIFESPAN=30d
POS_STRAPI__UP_IDLE_REFRESH_TOKEN_LIFESPAN=30d

# ----- services/core -----
# The Koa/Knex backend serving the same descriptor contract as services/strapi.
# RUTBA_BACKEND (strapi|core|both) decides which one listens; both read the same
# DATABASE_* values, and core falls back to the POS_STRAPI__ prefix for anything
# it does not own — except PORT and HOST, which it must never inherit.
CORE__PORT=4020

# ----- apps/admin/auth -----
AUTH__PORT=4003

# ----- apps/inventory/stock -----
STOCK__PORT=4001

# ----- apps/sales/pos -----
POS__PORT=4002

# ----- apps/content/storefront -----
STOREFRONT__PORT=4000
STOREFRONT__NEXTAUTH_SECRET=change_me_nextauth_secret
STOREFRONT__NEXTAUTH_URL=http://localhost:4000
STOREFRONT__GOOGLE_CLIENT_KEY=
STOREFRONT__GOOGLE_SECRET_KEY=

# ----- apps/sales/portal -----
PORTAL__PORT=4004

# ----- apps/sales/orders -----
ORDERS__PORT=4013

# ----- apps/sales/rider -----
RIDER__PORT=4012

# ----- apps/sales/crm -----
CRM__PORT=4005

# ----- apps/people/hr -----
HR__PORT=4006

# ----- apps/people/ess -----
ESS__PORT=4015

# ----- apps/finance/accounts -----
ACCOUNTS__PORT=4007

# ----- apps/finance/payroll -----
PAYROLL__PORT=4008

# ----- apps/content/cms -----
CMS__PORT=4009

# ----- apps/content/social -----
SOCIAL__PORT=4011

# ----- apps/inventory/manufacturing -----
MANUFACTURING__PORT=4014

# ----- apps/sales/marketplace -----
# The app AND the standalone sync worker (rutba_marketplace_worker) both read
# these. The worker talks to Strapi server-to-server with a FULL-ACCESS API
# token — api-pro skips token-authenticated requests.
MARKETPLACE__PORT=4016
MARKETPLACE__STRAPI_API_URL=http://127.0.0.1:4010/api
MARKETPLACE__STRAPI_SERVICE_TOKEN=
MARKETPLACE__PUBLIC_URL=http://localhost:4016
MARKETPLACE__WORKER_ENABLED=true
MARKETPLACE__JOBS_BACKEND=inproc
MARKETPLACE__CRON_ORDERS_RULE=*/15 * * * *
MARKETPLACE__CRON_INVENTORY_RULE=*/30 * * * *
MARKETPLACE__CRON_REFRESH_RULE=0 */4 * * *
MARKETPLACE__DARAZ_APP_KEY=
MARKETPLACE__DARAZ_APP_SECRET=
MARKETPLACE__DARAZ_REGION=pk
# Optional host overrides; blank = the regional default for DARAZ_REGION.
# API_HOST = business REST, AUTH_URL = browser authorize page, TOKEN_HOST =
# the auth gateway the token create/refresh calls POST to (defaults to the
# business host — set it if the token exchange 404s or returns a non-zero code).
MARKETPLACE__DARAZ_API_HOST=
MARKETPLACE__DARAZ_AUTH_URL=
MARKETPLACE__DARAZ_TOKEN_HOST=

# ----- apps/inventory/control -----
CONTROL__PORT=4017

# ----- apps/admin/seed -----
SEED__PORT=4018

# ----- apps/content/campaigns -----
# 4019 is the canonical port; a host whose media-fileserver already owns 4019
# (the LAN box) overrides this to a free port in its master env.
CAMPAIGNS__PORT=4019

# ----- apps/content/mail -----
MAIL__PORT=4021

# ----- apps/admin/console -----
CONSOLE__PORT=4022

# ----- apps/sales/helpdesk -----
HELPDESK__PORT=4023
